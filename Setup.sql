USE nikola;

-- CREATE TABLE jobs(
--     title VARCHAR(255) NOT NULL UNIQUE,
--     description VARCHAR(255) NOT NULL UNIQUE
-- );

-- INSERT INTO jobs
-- ( name, description )
-- VALUES
-- ("Deh Contracting Jerb", "Its a job I guess")